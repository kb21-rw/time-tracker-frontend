export const API_URL = import.meta.env.VITE_API_URL

// Time constants
export enum TimeConstants {
    SECONDS_PER_MINUTE = 60,
    MINUTES_PER_HOUR = 60,
    HOURS_PER_DAY = 24,
    MS_PER_SECOND = 1000,
}
